schema_version: 1
name: user
version: '1.0'
description: "Configures the user and permissions. "

envs:
- name: "HOME"
  value: "/home/ol"

packages:
  install:
  - shadow-utils # groupadd


execute:
- script: configure.sh

run:
  user: 1001
  workdir: "/home/ol"